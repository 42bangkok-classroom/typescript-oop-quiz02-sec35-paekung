import axios from 'axios';

interface Comments {
  postId: number;
  id: number;
  name: string;
  email: string;
  body: string;
}

export async function countCommentsByPost() {
  try {
    
  } catch (error) {
    return [];
  }
}
